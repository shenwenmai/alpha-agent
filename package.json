{
  "name": "baccarat-agent-panel",
  "version": "1.0.0",
  "description": "博弈风控三Agent系统 — 独立API服务",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "test": "node --require tsx/cjs --test tests/fallback.test.ts tests/consensus.test.ts",
    "test:fallback": "node --require tsx/cjs --test tests/fallback.test.ts",
    "test:consensus": "node --require tsx/cjs --test tests/consensus.test.ts"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
